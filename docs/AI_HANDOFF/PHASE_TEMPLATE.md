# Phase `<0-4>`：`<阶段标题>`

> 状态：`planned | in_progress | blocked | accepted | superseded`
> 负责人/代理：`<名称或会话标识>`
> 创建日期：`YYYY-MM-DD`
> 最后更新：`YYYY-MM-DD HH:mm +时区`
> 前置文档：`<相对路径 | 无（阶段首份）>`
> 后继文档：`<相对路径 | 暂无>`
> 对应路线图：`docs/WINDOWS_ROADMAP.md#<锚点>`

## 0. 本次范围

### 目标

- `<本次要达成的可验证结果>`

### 包含

- `<明确包含的功能、层或文件范围>`

### 不包含

- `<明确排除的内容>`
- `不修改仓库根目录 HANDOVER.md。`
- `不回退、覆盖、暂存或删除其他代理的现有改动。`

### 事实、计划与假设

- **事实**：`<已验证事实及证据>`
- **计划**：`<尚未实施的目标>`
- **假设**：`<待验证判断及验证方法>`

## 1. 基线提交

- 仓库：`<仓库名/路径>`
- 分支：`<branch>`
- 上游：`<remote/branch | 无>`
- 基线提交（完整 SHA）：`<40 位 SHA>`
- 基线提交摘要：`<git log -1 --oneline>`
- 接手时间：`YYYY-MM-DD HH:mm +时区`
- 基线是否已推送：`是 | 否 | 未确认`

接手时执行：

```powershell
git status --short --branch
git rev-parse HEAD
git log -1 --oneline
```

接手时工作区摘要：

```text
<粘贴脱敏后的关键状态；不要省略与本阶段可能重叠的文件>
```

接手前已存在、非本阶段所有的改动：

| 路径 | 状态 | 推定所有者/来源 | 本阶段处理方式 |
| --- | --- | --- | --- |
| `<path>` | `M / A / D / ??` | `<未知或代理名>` | `不触碰 / 协调后处理` |

## 2. 变更文件

只列本阶段实际拥有的变更；接手前已有且未触碰的文件不要混入。

| 路径 | 变更类型 | 变更目的 | 是否含迁移 | 对应提交 |
| --- | --- | --- | --- | --- |
| `<path>` | `新增 / 修改 / 删除` | `<一句话说明>` | `是 / 否` | `<SHA | 未提交>` |

生成物、缓存或本地文件：

- `<无；或列出路径、用途、是否应提交/清理>`

## 3. 设计决策

| ID | 决策 | 备选方案 | 选择理由 | 兼容性/风险影响 |
| --- | --- | --- | --- | --- |
| `D-001` | `<采用的方案>` | `<被放弃方案>` | `<证据和权衡>` | `<影响>` |

需要后续复审的决策：

- `<决策 ID、触发条件、复审负责人>`

## 4. 迁移

没有迁移时也必须填写“无”，并说明为什么。

### 配置迁移

- 旧格式/默认值：`<内容 | 无>`
- 新格式/默认值：`<内容 | 无>`
- 自动迁移步骤：`<步骤 | 无>`
- 失败回退：`<行为 | 无>`

### 数据与缓存迁移

- 影响范围：`<内容 | 无>`
- 备份要求：`<内容 | 无>`
- 幂等性：`<可重复执行情况 | 不适用>`

### IPC、API 或类型迁移

- 旧契约：`<内容 | 无>`
- 新契约：`<内容 | 无>`
- 兼容窗口：`<内容 | 无>`
- 两端更新顺序：`<内容 | 无>`

### 构建与依赖迁移

- 依赖/锁文件变化：`<内容 | 无>`
- 工具链要求：`<内容 | 无>`
- CI/打包变化：`<内容 | 无>`

## 5. 验收项映射

| 路线图验收项 | 结果 | 证据 | 备注 |
| --- | --- | --- | --- |
| `<原验收项或简写>` | `PASS / FAIL / SKIPPED` | `<日志、截图、测试或文件>` | `<说明>` |

## 6. 验证命令与结果

规则：

- 只保留实际相关的命令。
- 实际未执行的命令必须写 `SKIPPED` 和原因。
- 记录退出码、关键输出和运行环境；不得写入凭据或私人 URL。

环境：

- Windows 版本：`<版本/构建号>`
- 架构：`x64 | arm64 | 其他`
- Node/npm：`<版本 | 未使用>`
- Rust/Cargo：`<版本 | 未使用>`
- 目标运行时：`Electron | Tauri | Qt/mpv | 其他`
- 其他关键版本：`<内容>`

| 时间 | 命令 | 结果 | 退出码 | 关键输出/证据 |
| --- | --- | --- | --- | --- |
| `YYYY-MM-DD HH:mm` | `git diff --check` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `npm.cmd run check:local-decode` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `npm.cmd run check:no-planned-ui` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `npm.cmd run check:electron-commands` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `npm.cmd run check:workspace` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `npm.cmd run build` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `cargo check --manifest-path .\src-tauri\Cargo.toml --all-targets` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |
| `YYYY-MM-DD HH:mm` | `<目标运行时 build/package/smoke>` | `PASS / FAIL / SKIPPED` | `<code>` | `<摘要>` |

人工验证：

| 场景 | 步骤 | 预期 | 实际 | 结果 |
| --- | --- | --- | --- | --- |
| `<场景>` | `<可复现步骤>` | `<预期>` | `<实际>` | `PASS / FAIL / SKIPPED` |

## 7. 未解决问题

| ID | 严重度 | 问题与证据 | 影响 | 临时措施 | 解除条件/负责人 |
| --- | --- | --- | --- | --- | --- |
| `U-001` | `阻塞 / 高 / 中 / 低` | `<问题和证据>` | `<影响>` | `<措施 | 无>` | `<条件/负责人>` |

已知但本阶段不处理：

- `<问题、原因、建议进入的 Phase>`

## 8. 下一步

按优先级给出可直接执行的动作。

1. `<动作>`
   - 文件入口：`<path>`
   - 前置条件：`<条件>`
   - 预期结果：`<结果>`
   - 完成判据：`<可验证标准>`
2. `<动作>`
   - 文件入口：`<path>`
   - 前置条件：`<条件>`
   - 预期结果：`<结果>`
   - 完成判据：`<可验证标准>`

建议下一位代理先阅读：

1. `<相对路径>`
2. `<相对路径>`

## 9. 回滚方法

### 回滚边界

- 本阶段提交：`<SHA 列表 | 尚未提交>`
- 本阶段拥有文件：`<路径列表>`
- 明确不得触碰：`<接手前改动或其他代理文件>`

### 代码回滚

1. 先重新检查共享工作区和目标提交：

   ```powershell
   git status --short --branch
   git show --stat <commit>
   ```

2. 对已经共享的本阶段提交，优先在确认范围后使用可审计的：

   ```powershell
   git revert <commit>
   ```

3. 对未提交改动，只手工反向修改本阶段拥有的文件。
4. 不使用 `git reset --hard`、`git checkout -- <path>` 或批量清理来回滚共享工作区。

### 配置、数据与产物回滚

- 配置：`<步骤 | 无>`
- 数据/缓存：`<步骤 | 无>`
- 安装包/运行时：`<步骤 | 无>`
- 回滚后验证命令：`<命令>`
- 不可逆影响：`<内容 | 无>`

## 10. 参考与许可证记录

参考项目：

- LinPlayer：AGPL-3.0。

本阶段仅参考的思想：

- `<用户可观察行为、功能拆分、异常场景或测试思路；无则写“无”>`

独立实现证据：

- `<本项目既有接口、独立设计记录、测试先行记录等>`

代码或资源复用：

- `无。未复制或逐行改写参考项目的源代码、测试、注释、配置、文案、布局实现、图标、图片、字体、着色器、补丁或二进制资源。`
- `<如不是“无”，立即停止相关交付并记录待许可证评审，不得自行删除本提示。>`

## 11. 提交与最终快照

本阶段提交：

| SHA | 提交主题 | 包含范围 | 已推送 |
| --- | --- | --- | --- |
| `<SHA>` | `<subject>` | `<范围>` | `是 / 否 / 未确认` |

结束时状态：

```powershell
git status --short --branch
git rev-parse HEAD
git diff --check
```

结束时工作区摘要：

```text
<记录本阶段遗留改动与仍属其他代理的改动>
```

接手结论：

- 阶段状态：`planned | in_progress | blocked | accepted | superseded`
- 可否由下一位代理直接继续：`是 | 否`
- 若否，唯一阻塞条件：`<条件>`
