# 2026-06-01 18:21 设置页服务器/线路 UI 收敛

## 变更

- 服务器列表项取消内层玻璃卡片外观，改为设置面板内的列表行分隔，减少“矩形框套矩形框”的视觉噪音。
- 服务器编辑区的多线路配置改为轻量分隔线布局，线路高级设置不再额外绘制卡片边界。
- 保留现有添加服务器与线路编辑逻辑：新增服务器仍为追加记录，线路 URL 继续允许任意端口，User-Agent / Headers 仍收在每条线路的高级设置里。
- 播放链路未放松本机解码硬约束，构建过程继续执行 `check:local-decode`，禁止服务端转码入口回归。

## 验证

- `git diff --check`
- `npm.cmd run build`
- `npm.cmd run electron:build`
