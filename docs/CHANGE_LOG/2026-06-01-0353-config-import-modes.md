# 2026-06-01 03:53 配置导入模式

## 变更

- 设置页“备份与还原”把导入拆成“合并导入”和“替换导入”。
- 合并导入继续使用 `merge`，保留已有状态并按 id 合并服务器、账号和设置。
- 替换导入使用 `replace`，执行前弹出确认，避免误操作覆盖当前配置。
- 导入完成后的状态文案会区分“合并”或“替换”。

## 验证

- `npm.cmd run build`
- in-app Browser 冷刷新 `/settings?c=backup`，确认“导出配置 / 合并导入 / 替换导入”三个按钮可见。
