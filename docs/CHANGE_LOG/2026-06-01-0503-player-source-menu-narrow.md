# 播放源菜单窄屏切线修复

- **时间**：2026-06-01 05:03 (UTC+8)
- **动机**：真实账号回归时发现 Web Preview 窄屏下可从设置菜单打开“播放源”，但线路/媒体源面板被渲染在响应式隐藏的底栏按钮内，条目存在却没有可点击区域，导致无法实际切到第二条线路。
- **修改文件**：
  - `src/views/PlayerView.vue`：播放器打开弹层时刷新控制栏隐藏计时；任意播放器弹层打开期间不自动隐藏控制栏；播放源面板打开时临时取消自身 `medium` 宽度隐藏规则，确保窄屏下线路/媒体源条目有真实点击区域。
- **风险**：打开播放源菜单时，窄屏底栏会临时显示播放源按钮，可能带来轻微布局变化；关闭菜单后仍回到原有响应式收纳规则。
- **回滚**：撤回 `src/views/PlayerView.vue` 中控制栏隐藏计时与播放源 `data-hide-below` 绑定调整即可。
- **验证步骤**：
  - `npm.cmd run build`
  - `npm.cmd run electron:build`
  - `git diff --check`
  - in-app Browser `http://127.0.0.1:1421` 使用测试账号新增两条 443 线路并真实登录，确认自动识别为 Emby、服务器为追加而非覆盖、首页拉到 5 个媒体库。
  - 进入真实剧集并播放，HTML 视频返回 1920x1080、时长约 23 分 42 秒，用户点击后 `paused=false` 且播放时间持续推进。
  - 在窄屏播放器中通过“设置 → 播放源”打开面板，确认 Line 1 / Line 2 与多个媒体源条目有非零可点击尺寸；点击 Line 2 后菜单重新打开显示 Line 2 为 active，真实视频继续播放且无视频错误。
  - 敏感值扫描确认未写入测试账号、密码、token 或完整线路地址。
- **结果**：通过；真实服务器登录、媒体库、Web Preview 内嵌播放和窄屏播放源切线均已验证闭环。
