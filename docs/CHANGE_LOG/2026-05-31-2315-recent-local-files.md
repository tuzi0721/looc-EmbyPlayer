# 最近本地文件入口

- **时间**：2026-05-31 23:15 (UTC+8)
- **动机**：本地视频文件已经可以直接打开，但每次都必须重新走文件选择器；文件源能力里“历史/最近”仍缺一个轻量入口。
- **修改文件**：
  - `src/stores/localFiles.ts` — 新增最近本地文件 store，使用客户端 `localStorage` 保存最近 8 个路径，按打开时间排序，并做基础结构校验。
  - `src/components/common/AppSidebar.vue` — 打开本地文件后写入最近记录；侧边栏底部显示最近 3 个本地文件，可直接点击重开，也可一键清空。
- **风险**：最近文件路径只保存在当前客户端本地，不进入项目文件、Git、后端或同步服务；完整文件夹媒体库、跨设备同步和隐私开关后续另做。
- **回滚**：删除 `localFiles` store，移除侧边栏最近本地文件区，并把打开本地文件恢复为仅跳转播放器。
- **验证步骤**：
  - `npm.cmd run build`
  - `git diff --check`
  - in-app Browser 空状态侧边栏目检：确认“打开本地文件”和“设置”仍可见，最近区为空时不占位
  - `npm.cmd run electron:build`
- **结果**：通过；空状态侧边栏布局未被新增区域挤坏，Electron unpacked 包完整性检查通过。
