# 2026-06-10 23:50 播放器日志（开关 + 打开日志文件夹）

## 背景
按参考截图（`SETTINGS_REFERENCE_HILLSLITE.md`·调试）复刻「播放器日志」：开关 +
日志目录入口。

## 变更
- Rust：`player_log_enabled`（默认关）；`resolve_player_log_dir()` →
  `%APPDATA%/app.embyplayer/logs`（两运行时共用）；`spawn_mpv_ipc` 开启时注入
  `--log-file=<dir>/mpv-<时间戳>.log`；新命令 `open_player_log_dir`（确保目录并
  用资源管理器打开）。
- Electron：mpv 启动参数同步注入 `--log-file`；`open_player_log_dir` 等价实现
  （shell.openPath）。
- 设置页播放器面板：播放器日志开关（标注下次播放生效）+ 打开日志文件夹按钮。
- TS 类型 + 三处默认值 + Electron store。

## 验证
- `npm run build` 绿（7.36s）；`cargo check` 绿（23.7s）；`node --check` ×3 绿；
  Electron coverage 绿（113 cmd/109 handlers/5 no-op）；无 lint。
- 行为待真机：开启后播放一次 → logs 目录出现 mpv-*.log；按钮打开资源管理器。
