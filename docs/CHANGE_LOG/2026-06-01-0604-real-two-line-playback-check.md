# 真实双线路默认播放回归

- **时间**：2026-06-01 06:04 (UTC+8)
- **动机**：上一阶段已经提供设置页当前线路入口，需要用真实测试账号确认“设置页 active line”会影响后续播放器默认播放线路，并且播放器会话内切源仍可独立工作。
- **修改文件**：
  - `docs/CURRENT_STATE.md`：记录真实双线路默认播放与会话切源回归结果。
- **风险**：本阶段只更新验证文档，不改变运行时代码；验证使用的真实服务器状态只保留在当前 Web Preview 本地会话。
- **回滚**：删除本日志，并将 `docs/CURRENT_STATE.md` 恢复到上一阶段快照。
- **验证步骤**：
  - in-app Browser 1420 Web Preview 使用真实测试账号当前会话打开 `/settings?c=servers`。
  - 将当前真实服务器编辑为两条 443 线路并保存，确认第二线路追加到原服务器而非替换服务器列表。
  - 执行线路测活，确认两条线路返回真实秒级延迟。
  - 在设置页将第二线路设为当前，再打开真实媒体播放器，确认播放源菜单默认选中第二线路。
  - 在播放器播放源菜单中切到主线路，再切回第二线路，确认视频对象保持 1440×1080 / readyState 4。
  - `git diff --check`
  - 敏感关键字扫描确认未写入测试账号、密码、token 或完整线路地址。
- **结果**：通过；设置页服务器级 active line、播放器默认线路和播放器会话级切源已用真实服务器串起来验证。
