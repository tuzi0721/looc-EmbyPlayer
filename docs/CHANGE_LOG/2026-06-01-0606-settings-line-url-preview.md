# 设置页线路 URL 脱敏预览

- **时间**：2026-06-01 06:06 (UTC+8)
- **动机**：真实服务器回归时，设置页普通查看态会直接暴露完整线路 URL；这不利于截图、录屏或旁观场景，也和日志里避免写入完整线路地址的原则不一致。
- **修改文件**：
  - `src/views/SettingsView.vue`：线路列表查看态改为脱敏 URL 预览；本地 `localhost` / IP 线路保持完整显示，远端主机名仅保留少量可辨认字符，完整地址仍在编辑模式中可见。
  - `docs/CURRENT_STATE.md`：记录本轮隐私展示修复。
- **风险**：用户在普通列表里无法直接复制完整线路 URL，需要进入编辑模式查看；换来截图/录屏时更低的信息暴露风险。
- **回滚**：把线路列表展示从 `lineUrlPreview(l.baseUrl)` 改回 `l.baseUrl`，并删除本日志与状态段落。
- **验证步骤**：
  - `npm.cmd run build`
  - in-app Browser 1420 打开 `/settings?c=servers`，确认真实远端线路展示为脱敏预览，本地测试线路仍展示本地地址。
  - `git diff --check`
  - 敏感关键字扫描确认未写入测试账号、密码、token 或完整线路地址。
  - `npm.cmd run electron:build`
- **结果**：通过；设置页普通查看态不再把真实远端线路完整暴露出来。
