# 播放器播放按钮可识别

- **时间**：2026-06-01 05:53 (UTC+8)
- **动机**：真实服务器 Web Preview 回归进入播放器后，中央播放/暂停按钮在 DOM 与辅助技术中表现为空按钮，不利于用户悬停确认、自动化回归和可访问性识别。
- **修改文件**：
  - `src/views/PlayerView.vue`：播放/暂停主按钮新增动态 `title` 与 `aria-label`，随当前暂停状态显示“播放”或“暂停”。
  - `docs/CURRENT_STATE.md`：记录真实服务器回归结果和本轮按钮可识别性修复。
- **风险**：仅改变按钮可访问名称与悬停提示，不改变播放控制逻辑；若回滚，只会恢复为空图标按钮。
- **回滚**：移除播放按钮上的 `title` 与 `aria-label` 绑定，并将 `docs/CURRENT_STATE.md` 恢复到上一阶段快照。
- **验证步骤**：
  - in-app Browser 1420 Web Preview 使用真实测试账号当前会话确认服务器已连接、媒体库加载、电影列表与详情页可打开，进入播放器后拿到真实 1440×1080 HTML 视频对象。
  - 刷新播放器页并检查中央播放按钮可通过可访问名称“播放”定位。
  - `npm.cmd run build`
  - `git diff --check`
  - 敏感关键字扫描确认未写入测试账号、密码、token 或完整线路地址。
  - `npm.cmd run electron:build`
- **结果**：通过；真实服务器链路可进入 Web Preview 播放器并加载视频对象，播放/暂停主按钮不再是空按钮。
