# MPV 管道 + rustls 闪退修复 + 品牌对齐

- **时间**：2026-05-25 22:30 (UTC+8)
- **动机**：全量审计后按最佳实践修复 G3/G4/G5/G8/G10/G1/G2；解决打开闪退（rustls CryptoProvider panic）

---

## 修改摘要

| 区域 | 改动 |
|---|---|
| **build.rs** | 从 zhongfly 7z 下载 mpv → `vendor/mpv/`，复制到 `target/*/resources/mpv/` |
| **mpv/paths.rs** | `resolve_mpv_exe()` / `mpv_exists()` |
| **mpv/ipc.rs** | `--input-ipc-server` 命名管道 IPC（替代 fd://0） |
| **lib.rs** | `rustls::crypto::ring::default_provider().install_default()` 修 WebSocket 闪退 |
| **player.rs** | `detect_mpv` / `open_external` 命令 |
| **tauri.conf** | 品牌 Hills Lite；`bundle.active: false` 只产 exe |
| **前端** | 品牌统一；收藏/聚合路由+Sidebar；MpvBanner 挂载；Player back fire-and-forget |
| **library store** | `searching` / `clearSearch` 修 TopBar TS 错 |

---

## 风险

- 首次编译需联网下载 ~30MB mpv 7z
- mpv 版本 pinned 到 2026-05-20 zhongfly build

---

## 回滚

```powershell
git checkout HEAD -- src-tauri/ src/ index.html docs/
Remove-Item -Recurse src-tauri/vendor -ErrorAction SilentlyContinue
```

---

## 验证

- [x] `cargo build --release` 成功
- [x] `vendor/mpv/mpv.exe` 存在 (~113MB)
- [x] exe 启动 6s 无 `crash.log` 新条目
- [ ] 用户侧：登录 → 播放 → 返回

---

## 产物

```
src-tauri/target/release/emby-player.exe
src-tauri/target/release/resources/mpv/mpv.exe
```
