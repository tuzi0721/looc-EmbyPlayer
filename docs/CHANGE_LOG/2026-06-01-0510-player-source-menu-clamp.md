# 播放源菜单视口约束

- **时间**：2026-06-01 05:10 (UTC+8)
- **动机**：播放源菜单窄屏可点击后，继续目检发现弹层可能贴着底栏按钮定位，存在边缘跑出视口或被控制栏压住的风险，需要把菜单主体约束在播放器视口内。
- **修改文件**：
  - `src/views/PlayerView.vue`：920px 以下将播放源弹层改为固定定位，左右保留 16px；760px 以下控制栏双行时将弹层上移，避免覆盖底栏。
- **风险**：仅影响窄屏播放源菜单定位；宽屏仍使用原有相对按钮定位。
- **回滚**：撤回 `src/views/PlayerView.vue` 中 `.popup-menu--source` 的窄屏媒体查询即可。
- **验证步骤**：
  - `npm.cmd run build`
  - `npm.cmd run electron:build`
  - `git diff --check`
  - in-app Browser 真实播放页打开“设置 → 播放源”，确认 Line 2 与媒体源选中状态可见，弹层主体位于画面内且不被底栏遮挡。
  - 敏感值扫描确认未写入测试账号、密码、token 或完整线路地址。
- **结果**：通过；播放源菜单在真实播放页中可见、可读、可继续用于线路和媒体源确认。
